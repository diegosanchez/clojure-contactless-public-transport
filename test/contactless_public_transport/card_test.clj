(ns contactless-public-transport.card-test
  (:require [clojure.test :refer :all]
            [contactless-public-transport.card :refer :all]))

(deftest card-with-initial-
  (testing "FIXME, I fail."
    (is (= (fn-under-test) 1))))

